    # 信贷核心 — PRD 产品需求文档

    - **产品名称**：信贷核心 (Credit Core)
    - **版本**：v1.0
    - **数据库**：`sim_credit_core`
    - **产品负责人**：产品经理

    ## 一、产品范围

    信贷核心系统提供 **承载授信额度、支用（放款）、还款计划、还款流水、逾期状态的全生命周期** 的完整产品能力。

    **在范围内**：
    - 产品配置
- 授信创建
- 支用放款
- 生成还款计划
- 还款处理
- 逾期识别
- 结清/坏账

    **不在范围内**：
    - 属于上游系统的能力（如客户主档由 CIF 负责）
    - 属于下游系统的能力（如财务凭证由 finance 负责）
    - 属于数仓的能力（原始明细汇总由 dw 负责）

    ## 二、用户角色 & 权限

    | 角色 | 权限 |
    |---|---|
    | 系统对接账号 | API 读写（白名单） |
    | 业务操作员 | 台内操作 |
    | 审核员 | 只读 + 审批 |
    | 管理员 | 全部 + 配置 |
    | 合规官 | 只读 + 审计 |

    ## 三、核心用例

    ### UC-01 产品配置

**触发条件**：见 BRD 章节四
**前置条件**：上游系统已完成前置动作
**主流程**：
1. 接收业务请求
2. 参数校验 + 幂等去重
3. 调用领域服务处理
4. 持久化 + 发消息
5. 返回结果

**异常处理**：熔断降级；重试 3 次后转异步补偿。

### UC-02 授信创建

**触发条件**：见 BRD 章节四
**前置条件**：上游系统已完成前置动作
**主流程**：
1. 接收业务请求
2. 参数校验 + 幂等去重
3. 调用领域服务处理
4. 持久化 + 发消息
5. 返回结果

**异常处理**：熔断降级；重试 3 次后转异步补偿。

### UC-03 支用放款

**触发条件**：见 BRD 章节四
**前置条件**：上游系统已完成前置动作
**主流程**：
1. 接收业务请求
2. 参数校验 + 幂等去重
3. 调用领域服务处理
4. 持久化 + 发消息
5. 返回结果

**异常处理**：熔断降级；重试 3 次后转异步补偿。

### UC-04 生成还款计划

**触发条件**：见 BRD 章节四
**前置条件**：上游系统已完成前置动作
**主流程**：
1. 接收业务请求
2. 参数校验 + 幂等去重
3. 调用领域服务处理
4. 持久化 + 发消息
5. 返回结果

**异常处理**：熔断降级；重试 3 次后转异步补偿。

### UC-05 还款处理

**触发条件**：见 BRD 章节四
**前置条件**：上游系统已完成前置动作
**主流程**：
1. 接收业务请求
2. 参数校验 + 幂等去重
3. 调用领域服务处理
4. 持久化 + 发消息
5. 返回结果

**异常处理**：熔断降级；重试 3 次后转异步补偿。

### UC-06 逾期识别

**触发条件**：见 BRD 章节四
**前置条件**：上游系统已完成前置动作
**主流程**：
1. 接收业务请求
2. 参数校验 + 幂等去重
3. 调用领域服务处理
4. 持久化 + 发消息
5. 返回结果

**异常处理**：熔断降级；重试 3 次后转异步补偿。

### UC-07 结清/坏账

**触发条件**：见 BRD 章节四
**前置条件**：上游系统已完成前置动作
**主流程**：
1. 接收业务请求
2. 参数校验 + 幂等去重
3. 调用领域服务处理
4. 持久化 + 发消息
5. 返回结果

**异常处理**：熔断降级；重试 3 次后转异步补偿。


    ## 四、功能列表

    | 表/资源 | 提供的能力 |
    |---|---|
    | product | 详见 schema.sql |
| credit_limit | 详见 schema.sql |
| loan | 详见 schema.sql |
| loan_ledger | 详见 schema.sql |
| repayment_plan | 详见 schema.sql |
| repayment_actual | 详见 schema.sql |
| overdue_record | 详见 schema.sql |
| contract | 详见 schema.sql |
| fee_charge | 详见 schema.sql |
| loan_status_log | 详见 schema.sql |

    ## 五、流程图

    ```mermaid
    graph LR
        A[风控决策 risk_decision, 进件 loan_intake] --> B[信贷核心]
        B --> C[催收 collection, 财务 finance, 资金 funding]
        B --> D[(MySQL sim_credit_core)]
    ```

    ## 六、交互规则

    - **幂等性**：所有写接口幂等（客户端提供 idempotency-key）
    - **审计**：核心状态变更留 audit trail
    - **限流**：单客户 QPS ≤ 20，单接口 QPS ≤ 1000
    - **异常回滚**：分布式事务用 Saga，失败自动补偿
    - **异步优先**：非关键路径全部异步化
    - **降级策略**：依赖故障时按预定义降级方案继续提供有限能力

    ## 七、数据规则

    - 主键：见 schema.sql
    - 索引：详见 schema.sql
    - 唯一性：见 UNIQUE KEY
    - 状态机：核心表带 `status` 字段，转换需通过状态日志表记录

    ## 八、验收标准

    - [x] 覆盖 7 个核心用例
    - [x] 与上游/下游联调通过
    - [x] 单元测试覆盖率 ≥ 80%
    - [x] 通过安全测试
    - [x] 通过性能压测（QPS 达标）

    ## 九、非功能需求

    - **可用性**：99.99%
    - **性能**：P95 读 180ms，写 800ms
    - **安全**：全链路 TLS 1.3；等保 3.0

## 十、核心业务规则详细说明（重点系统必备）

### 10.1 幂等设计
所有写接口必须支持幂等，客户端传 `Idempotency-Key`，服务端 Redis 存 24 小时。

### 10.2 状态机
核心业务实体的状态转移需通过状态机管理，非法状态转移直接拒绝并记录。

### 10.3 事件发布
每个关键业务动作发布事件到 RocketMQ，事件包含：
- `event_id`（UUID）
- `event_type`
- `occurred_at`
- `payload`
- `trace_id`

### 10.4 灰度发布
新规则/新流程支持按客户 / 按渠道 / 按比例灰度。

### 10.5 数据完整性
跨系统同步采用最终一致性，通过每日对账脚本兜底。

### 10.6 全链路 trace
通过 `trace_id` 串联所有服务的日志，便于故障排查。

