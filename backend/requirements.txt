# ============================================================
# OntoMind Backend Requirements
#
# 2026-08-03 深度精简：项目只剩 AIDE（opencode Web UI 嵌入）+ 用户管理，
# 后端 3 个路由域 / 4 张表。原来五层业务域 / 专家团 / 算力调度 /
# 数据平台 / 知识库 / Agent Looper 的依赖已全部移除。
#
# 移除清单：alembic（本就不可用）/ aiomysql / redis / langchain /
#   langchain-openai / openai / numpy / pandas / celery / rdflib /
#   asyncssh / sqlglot / sqlparse / sse-starlette
# ============================================================

# --- FastAPI Core ---
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.3
pydantic-settings==2.7.0
python-multipart==0.0.18

# --- Database ---
sqlalchemy==2.0.36
pymysql==1.1.1

# --- Authentication ---
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4

# --- HTTP client（AIDE 探活 opencode serve/web）---
httpx==0.28.1

# --- 进程管理（AIDE 扫描 `opencode web` 进程；缺失时代码会优雅降级）---
psutil==6.1.1

# --- 算力管理（SSH + Docker）---
asyncssh==2.19.0
docker==7.1.0

# --- Utils ---
python-dotenv==1.0.1

# --- Dev ---
pytest==8.3.4
pytest-asyncio==0.24.0
pytest-cov==7.1.0
black==24.10.0
ruff==0.8.4
