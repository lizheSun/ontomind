# 前端环境变量样例
# 拷贝此文件到 frontend/.env 并按需修改

# 后端 API 基地址
VITE_API_BASE_URL=http://localhost:8000/api/v1

# --- OpenCode 对话工作台 ---
# 本机 opencode serve 的 URL（默认 127.0.0.1:4096）
# 启动命令：opencode serve --port 4096 --cors http://localhost:5173
VITE_OPENCODE_URL=http://127.0.0.1:4096
