# OntoMind Agent 平台 — 跨机工作交接（2026-07-12）

> **目的**：另一台电脑 `git pull` 后，按本文即可续作，无需回看长对话。  
> **分支**：`main`  
> **日期**：2026-07-12  
> **测试账号**：`admin` / `admin123`  
> **本机默认端口**：前端 `127.0.0.1:5173`，后端 `127.0.0.1:8000`

---

## 0. 一句话总结

今天落地了 **Agent 资源管理 + Studio + 对话工作台**，并把对话从「整段阻塞返回」改成 **OpenCode 流式执行 + SSE 实时推送**（thinking / tools / steps / message.delta）。

---

## 1. 今天干了什么（按产品能力）

| 能力 | 说明 | 入口 |
|------|------|------|
| 资源控制台 | 本机自动 `register-local` + OpenCode inventory；三栏：节点 / OpenCode+资源 / 详情；平台 Agent 编辑/发布/去对话 | `/agent-platform/resources` |
| Agent Studio | 快速模式配置 Agent；保存草稿/发布；自动绑本机 OpenCode runtime | `/agent-platform/agents/new/studio` |
| 对话工作台 | 选 Agent 聊天；列表内不做编辑发布（改去资源管理） | `/workspace` |
| 运行记录 | Run 列表 + SSE 时间线 | `/agent-platform/runs` |
| 真实 OpenCode | `opencode run --format json`，按行解析 JSONL | 后端 `OpenCodeChatService` |
| 流式 SSE | 发消息立刻返回 `run_id`；后台流式写事件；SSE 长连接推送 | `POST .../sessions/{id}/messages` + `GET .../runs/{id}/events` |

### 产品层级（已确认）

```
计算节点 → OpenCode 容器 → Agent / Skill / MCP
```

- Agent **真源**：`agents` 表（平台侧）
- OpenCode：DB 做控制面；冲突人工确认
- **编辑/发布在资源管理**；对话工作台只聊天

---

## 2. 流式架构（最新关键改动）

```
浏览器 POST /sessions/{id}/messages
  → 立刻返回 { run_id, queued: true }
  → BackgroundTasks 执行 RunService.execute()
       → OpenCodeChatService.stream_prompt() 按行读 JSONL
       → map_opencode_event() → agent_run_events（每条 commit）
浏览器 GET /runs/{id}/events (SSE 长连接)
  → 轮询新事件直到 run.completed / failed / cancelled
前端 useAgentStream + timelineReducer
  → 聊天气泡 message.delta + 右侧 thinking/tool/step
```

测试仍可用 `metadata.force_stub=true` **同步**跑 stub（不调 OpenCode）。

---

## 3. 数据库：新增 / 修改

### 3.1 Alembic 链（按顺序）

| Revision | 文件 | 内容 |
|----------|------|------|
| `2026071201` | `2026071201_add_agent_looper_tables.py` | 早期 looper 表（已在历史） |
| `2026071202` | `2026071202_add_credentials_and_audit_logs.py` | **新增** `credentials`、`audit_logs` |
| `2026071203` | `2026071202_agent_platform_lifecycle.py`（注意文件名与 revision 不一致） | 生命周期域 |
| `2026071204` | `2026071204_node_connections_discovery.py` | 节点连接 + 发现 |

### 3.2 新增表

| 表 | 用途 |
|----|------|
| `credentials` | 加密凭据（SSH 等） |
| `audit_logs` | 安全审计 |
| `agent_versions` | Agent 不可变配置快照 |
| `agent_deployments` | 部署状态机 |
| `agent_sessions` | 会话 |
| `agent_messages` | 有序消息 |
| `agent_run_steps` | Run 步骤 |
| `agent_run_events` | Run 事件流（SSE 真源） |
| `agent_approvals` | 工具审批 |
| `agent_eval_defs` | 评测定义（若 migration 含） |
| `node_connections` | 节点连接（local/ssh + managed_roots） |
| `discovery_runs` | 发现任务 |
| `discovery_items` | 发现预览项 |

### 3.3 改动表

| 表 | 变更 |
|----|------|
| `agents` | +`owner_user_id`、`current_version_id` 等 |
| `agent_runs` | status 从 enum → string；+`agent_version_id`/`session_id`/`strategy`/`kind`/`input`/`output`/`state_version` 等 |
| `compute_nodes` | +`address`/`architecture`/`environment`/`last_heartbeat_at`/`last_scan_at` 等 |
| `skills` / `mcps` | 小幅字段扩展（平台关联） |

### 3.4 另一台电脑初始化 DB

```bash
cd backend
source venv/bin/activate   # 或新建 venv 后 pip install -r requirements.txt
# 配置 .env：DB_URL、FERNET_KEY（必填，否则凭据加密不可用）
alembic upgrade head
# 若 create_all 与 alembic 漂移，可跑：
python -m app.scripts.repair_agent_platform_schema
# 或：
python scripts/repair_agent_platform_schema.py
```

生成 Fernet：

```bash
python backend/scripts/gen_fernet.py
# 写入 backend/.env → FERNET_KEY=...
```

---

## 4. 关键代码位置

### 后端

| 路径 | 说明 |
|------|------|
| `backend/app/api/v1/agent_platform/` | REST：agents/sessions/runs/nodes/discoveries/... |
| `backend/app/services/agent_platform/` | 业务：session/run/opencode_chat/inventory/discovery/... |
| `backend/app/services/agent_platform/opencode_chat.py` | OpenCode CLI 流式 + 事件映射 |
| `backend/app/services/agent_platform/run.py` | `execute_opencode_streaming` + stub |
| `backend/app/services/agent_platform/session.py` | `send_message` 立即返回 + `execute_run_in_background` |
| `backend/app/db/models/agent_platform_model.py` | ORM |
| `backend/app/connectors/` | local/ssh 命令执行 |
| `backend/tests/agent_platform/` | 单测（含 SSE / force_stub） |

### 前端

| 路径 | 说明 |
|------|------|
| `frontend/src/pages/agent-platform/` | ResourcesConsole / Studio / ChatWorkspace / Runs |
| `frontend/src/hooks/useAgentStream.ts` | SSE 消费 + 重连 |
| `frontend/src/stores/agentPlatformStore.ts` | 状态 |
| `frontend/src/services/agentPlatform.service.ts` | API |

### 文档

| 路径 | 说明 |
|------|------|
| `docs/agent-platform/01-product-spec.md` | 产品规格 |
| `docs/agent-platform/02-prototype-spec.md` | 原型 |
| `docs/agent-platform/03-technical-design.md` | 技术方案 |
| `docs/agent-platform/HANDOFF-2026-07-12.md` | **本文** |
| `AGENT_LOG.md` | 根目录工作记录追加条目 |

---

## 5. 环境依赖（另一台电脑）

1. **MySQL**（或项目配置的 DB）与本机 `.env` 对齐，或新建库后 `alembic upgrade head`
2. **Python venv** + `pip install -r backend/requirements.txt`（含 `sse-starlette`）
3. **Node** + 前端 `npm install`
4. **opencode CLI**（对话真执行需要）：`which opencode`，配置 `~/.config/opencode`
5. **FERNET_KEY** 环境变量
6. 若要复用本机已有用户/数据：迁移 MySQL dump；否则用 `admin/admin123` 或重新 seed

### 启动

```bash
# 后端
cd backend && source venv/bin/activate
uvicorn app.main:app --reload --host 127.0.0.1 --port 8000

# 前端
cd frontend && npm run dev
```

### 冒烟

```bash
cd backend && ./venv/bin/python -m pytest tests/agent_platform/ -q
# 浏览器：登录 → 资源管理自动发现 → Studio 发布 → 对话发「1+1」应看到 SSE 流式输出
```

---

## 6. 已知行为 / 坑

| 项 | 说明 |
|----|------|
| OpenCode 单次约 30–40s | 现在不阻塞 HTTP；看 SSE |
| stub vs 真执行 | 测例 `force_stub`；UI 默认走 OpenCode |
| SSE session factory | 测试覆盖 `get_session_factory`；长连接每轮新开 DB session |
| 后台任务独立 Session | `execute_run_in_background` 用 `SessionLocal()`，避免请求 session 关闭后挂掉 |
| 编辑发布入口 | 只在资源管理 / Studio；工作台不提供 |
| 文件名陷阱 | migration 文件 `2026071202_agent_platform_lifecycle.py` 的 revision 实际是 `2026071203` |

---

## 7. 建议续作（未做完 / 可优化）

- [ ] OpenCode 更多事件类型对齐（若 CLI 输出 tool/reasoning 字段有变）
- [ ] 对话页展示 SSE 连接状态（connecting/open）
- [ ] WebSocket 备选（当前 SSE 已够用）
- [ ] 跨机若无本机 OpenCode，需先装 CLI 或改绑远程节点
- [ ] 清理/归档 `.blueprint/` 运行时产物（pids/logs 不必提交）

---

## 8. Git 迁移步骤（另一台）

```bash
git fetch origin
git checkout main
git pull origin main
# 然后按 §3 / §5 装依赖、升库、起服务
```

读完本文 + `AGENT_LOG.md` 当日条目即可继续开发。
